name: Security Gates

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  python-security:
    name: Python tests and dependency audit
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install HomeGuard and test tooling
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .[dev]
          python -m pip install pip-audit

      - name: Run unit tests
        run: python -m pytest -q

      - name: Run pip audit
        run: pip-audit

      - name: Run HomeGuard release security preflight
        run: python scripts/security_release_gate.py

  node-security:
    name: Electron dependency audit
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      - name: Run production npm audit
        run: npm audit --omit=dev --audit-level=high

      - name: Report Electron build-tool audit findings
        run: |
          npm audit --audit-level=high || true
          echo "Electron/electron-builder are audited above as build tooling. High findings here should be handled by regenerating package-lock.json after validating the Electron major-version upgrade."

      - name: Run Electron smoke test if available
        run: npm run smoke --if-present
